import { formatCurrency } from '@/lib/currency'
export function ClassificationBreakdown({ data }: { data: { essential: number; flexible: number; essentialPercent: number; flexiblePercent: number } }) { return <section className="panel report-section"><p className="eyebrow">Spending mix</p><h2>Essential vs flexible</h2><div className="report-grid"><p>Essential<strong>{formatCurrency(data.essential)}</strong><small>{data.essentialPercent.toFixed(1)}%</small></p><p>Flexible<strong>{formatCurrency(data.flexible)}</strong><small>{data.flexiblePercent.toFixed(1)}%</small></p></div></section> }
